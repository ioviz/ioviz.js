describe "AdjacentListStream", ->

  load_modules(
    "app/streams/graph/adjacent_list_stream"
  )

  context "create instance", ->
    
    before ->
      @instance = new AdjacentListStream

    it "", ->
      
  
